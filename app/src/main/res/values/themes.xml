<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CashTrack" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your light theme here as a dark theme. -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorPrimary">@color/accent_yellow</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorSurface">@color/card_background</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorControlNormal">@color/text_secondary</item>
    </style>

    <style name="Theme.CashTrack" parent="Base.Theme.CashTrack" />

    <style name="Widget.App.BottomAppBar" parent="Widget.MaterialComponents.BottomAppBar.Colored">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.BottomAppBar</item>
    </style>

    <style name="ThemeOverlay.App.BottomAppBar" parent="">
        <item name="colorOnSurface">@color/white</item>
    </style>

</resources>